# Postgres connection string for Prisma
DATABASE_URL="postgresql://cklee@localhost:5432/overflow_inventory?schema=public"

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-string"

# Optional: omit DATABASE_URL only if you intentionally want mock-data UI fallbacks
# (Prisma CLI commands always need DATABASE_URL)
